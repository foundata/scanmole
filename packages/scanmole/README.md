# scanmole

The ScanMole CLI scanning engine: scan documents from a SANE scanner straight to a searchable PDF. Stdlib-only, made for servers, scripts and as the backend of `scanmole-gui`.

Full documentation, screenshots and the FAQ live in the [ScanMole project README](https://github.com/foundata/scanmole#readme).
